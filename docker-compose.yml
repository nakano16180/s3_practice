version: "2"
services:
  minio:
    container_name: minio
    image: minio/minio
    ports:
      - "9000:9000"
    volumes:
      - ./data:/export
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    command: server /export
    restart: always
  mc:
    container_name: mc
    image: minio/mc
    ports:
      - "9001:9001"
    environment:
      MINIO_PORT: 9000
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    entrypoint: [""]
    command:
      - /bin/sh
      - -c
      - |
        mc config host add minio http://minio:$${MINIO_PORT} $${MINIO_ROOT_USER} $${MINIO_ROOT_PASSWORD} --api S3v4
        mc admin trace minio
    depends_on:
      - minio
    restart: always
networks:
  default:
    external:
      name: minio-lambda-net
